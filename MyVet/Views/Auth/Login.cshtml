@model MyVet.Domain.Dto.UserDto


@{
    ViewData["Title"] = "Authentication"; <!--Agrega un titulo-->
    Layout = "_LayoutLogin"; <!--Especificar el layout que debe tener-->
}
<br />
<br />


<div class="row">
    <div class="animate__animated animate__backInLeft col-md-8 offset-2">
        <div class="card-header">
            <h3>  <strong>Iniciar Sessión</strong> </h3>  <!--Strong pone negrilla -->
        </div>
        <div class="card-body">
            <section>
                <form id="account" asp-controller="Auth" asp-action="Login" method="post"> <!--llama al controlador y al metodo de dicho controlador -->
                    <h4> Iniciar sessión con cuenta registrada</h4>
                    <hr />

                    <div asp-validation-summary="ModelOnly" class="text-danger"></div> <!--Mostrar los mensajes de error--->
                    <div class="form-group">
                        <label asp-for="UserName"> </label>
                        <input asp-for="UserName" type="text" maxlength="200" class="form-control" />
                        <span asp-validation-for="UserName" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="Password"> </label>
                        <input asp-for="Password" type="password" maxlength="200" class="form-control" />
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <input type="submit" value="Acceder" class="btn btn-primary" /> |

                        <a asp-action="Register" class="btn btn-info">Registrarse</a>
                    </div>
                </form>
            </section>
        </div>
    </div>
</div>



@*Validar datanotation*@
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}